# All global modifiers are here. They are applied from certain game-features.
# Effects are fully scriptable here.
# These names can NOT be removed or changes, as the code uses them....

Generic_license = {
	valid_relation_trigger = {
		FROM = {
			NOT = { has_war_with = ROOT }
		}
	}

	license_purchase_cost = 0 #base cost reduction
	ai_license_acceptance = 15 #more to AI acceptance value.
	license_production_speed = 0.20 #speed  increase for being a license
	license_tech_difference_speed = 0.20 #less cost increase for each tech level between the license and own tech level 
}