ideas = {

	economic_system = { 
		law = yes
		use_list_view = yes
		
		free_economy = {
		
			cost = 200
			removal_cost = -1
			
			available = {
				NOT = { 
					has_government = fascism
					has_government = communism
				}
				if = {
					limit = { has_elections = yes }
					OR = {
						has_country_flag = ATE_elections_war
						custom_trigger_tooltip = {
							tooltip = ATE_more_100_parlament_tt
							if = {
								limit = { has_government = democratic }
								check_variable = { dem_parl > 100 }
							}
							if = {
								limit = { has_government = liberalism }
								check_variable = { lib_parl > 100 }
							}
							if = {
								limit = { has_government = socialism }
								check_variable = { soc_parl > 100 }
							}
							if = {
								limit = { has_government = centrism }
								check_variable = { cent_parl > 100 }
							}
							if = {
								limit = { has_government = neutrality }
								check_variable = { neut_parl > 100 }
							}
						}
					}
				}
			}
			
			modifier = {
				research_speed_factor = 0.1
				production_speed_industrial_complex_factor = -0.2
				production_speed_arms_factory_factor = -0.2
				trade_opinion_factor = 0.2
			}
			
			on_add = { custom_effect_tooltip = ate_free_economy_tt }
			
			default = yes
			cancel_if_invalid = no
			
			ai_will_do = {
   				factor = 2
   			}
		}
		
		invervention_economy = {
			
			available = {
				NOT = { has_government = communism }
				if = {
					limit = { has_elections = yes }
					OR = {
						has_country_flag = ATE_elections_war
						custom_trigger_tooltip = {
							tooltip = ATE_more_100_parlament_tt
							if = {
								limit = { has_government = democratic }
								check_variable = { dem_parl > 100 }
							}
							if = {
								limit = { has_government = liberalism }
								check_variable = { lib_parl > 100 }
							}
							if = {
								limit = { has_government = socialism }
								check_variable = { soc_parl > 100 }
							}
							if = {
								limit = { has_government = fascism }
								check_variable = { fas_parl > 100 }
							}
							if = {
								limit = { has_government = centrism }
								check_variable = { cent_parl > 100 }
							}
							if = {
								limit = { has_government = neutrality }
								check_variable = { neut_parl > 100 }
							}
						}
					}
				}
			}
			
			cost = 200
			removal_cost = -1
			
			modifier = {
				research_speed_factor = 0.05
				political_power_factor = 0.05
				production_speed_industrial_complex_factor = -0.1
				production_speed_arms_factory_factor = -0.1
				trade_opinion_factor = 0.1
			}
			
			on_add = { custom_effect_tooltip = ate_inter_economy_tt }
			
			cancel_if_invalid = no

			ai_will_do = {
   				factor = 5
   			}
		}
		
		corporativist_economy = {
			
			available = {
				OR = {
					has_government = fascism
					has_government = neutrality
				}
				if = {
					limit = { has_elections = yes }
					OR = {
						has_country_flag = ATE_elections_war
						custom_trigger_tooltip = {
							tooltip = ATE_more_100_parlament_tt
							if = {
								limit = { has_government = fascism }
								check_variable = { fas_parl > 100 }
							}
							if = {
								limit = { has_government = neutrality }
								check_variable = { neut_parl > 100 }
							}
						}
					}
				}
			}

			cost = 200
			removal_cost = -1
			
			modifier = {
				research_speed_factor = 0.025
				political_power_factor = 0.1
			}
			
			on_add = { custom_effect_tooltip = ate_corp_economy_tt }
			
			cancel_if_invalid = no

			ai_will_do = {
   				factor = 5
   			}
		}
		
		plan_economy = {
			
			available = {
				has_government = communism
				if = {
					limit = { has_elections = yes }
					OR = {
						has_country_flag = ATE_elections_war
						custom_trigger_tooltip = {
							tooltip = ATE_more_100_parlament_tt
							if = {
								limit = { has_government = communism }
								check_variable = { com_parl > 100 }
							}
						}
					}
				}
			}

			cost = 200
			removal_cost = -1
			
			modifier = {
				research_speed_factor = -0.1
				stability_factor = 0.1
				political_power_factor = 0.1
				production_speed_industrial_complex_factor = 0.1
				production_speed_arms_factory_factor = 0.1
				trade_opinion_factor = -0.1
			}
			
			on_add = { custom_effect_tooltip = ate_plan_economy_tt }
			
			cancel_if_invalid = no

			ai_will_do = {
   				factor = 5
   			}			
   		}
	}
}